import 'package:flutter/material.dart';

class AppsAndTeamsPage extends StatelessWidget {
  const AppsAndTeamsPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
